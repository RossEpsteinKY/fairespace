{
  "name": "my-project",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "postinstall": "npm --cwd fairespace/fs_be --production=false install",
    "build": "yarn --cwd server serve"
  }
}
